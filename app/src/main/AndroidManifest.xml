<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fourinrowapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.FourInRowApp">
        <activity android:name=".PagesPackage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PagesPackage.MainMenu" />
        <activity android:name=".PagesPackage.GameStart" />
        <activity android:name=".PagesPackage.ScoresActivity" />
        <activity android:name=".PagesPackage.LoginActivity" />
        <activity android:name=".PagesPackage.RegisterActivity" />
    </application>

</manifest>